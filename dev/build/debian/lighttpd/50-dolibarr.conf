# Alias for ZionOne directory
alias.url += (
	"/ZionOne" => "/usr/share/ZionOne/htdocs",
)

# Disallow access to libraries
#$HTTP["url"] =~ "^/ZionOne/libraries" {
#    url.access-deny = ( "" )
#}

# Limit access to setup script
#$HTTP["url"] =~ "^/ZionOne/setup" {
#	auth.backend = "htpasswd"
#	auth.backend.htpasswd.userfile = "/etc/ZionOne/htpasswd.setup"
#	auth.require = (
#		"/" => (
#			"method" => "basic",
#			"realm" => "ZionOne Setup",
#			"require" => "valid-user"
#		)
#	)
#}
